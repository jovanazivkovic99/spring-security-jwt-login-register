server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: password
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      springframework:
        security:
          DEBUG